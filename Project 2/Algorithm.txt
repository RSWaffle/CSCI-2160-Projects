display the project info with putstring
putstring how many values
getstring to convert into value? - put into variable
display each value message anter after each one
getstring loop  x times into values array

loop:
sum all values

store into a variable
display information

divide the sum variable by x
display the resulting value with whole number average message

Euclidean division, store in varible, display

use cmp and jp for comparing the values for least and greatest. Refer to dataVlasCMP.asm

display reverse order message
put the values into a stack
loop:
pop 
diplay what was just poped
